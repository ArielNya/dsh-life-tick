# dsh-life-tick bundle layer.
#
# Declared via `dsh.bundle.patch` in package.json, so
# `dsh plugin add github:ArielNya/dsh-life-tick` mounts this insert
# automatically. User-level overrides live in <dshHome>/life-tick.json.
- insert:
    - id: dsh-life-tick
      name: dsh-life-tick
